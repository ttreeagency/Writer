TYPO3:
  Neos:

    typoScript:
      autoInclude:
        'Ttree.Writer': TRUE

    userInterface:
      editPreviewModes:
        rawContent:
          title: 'Writer'

Ttree:
  Writer:

     singleCollection: ~
     # To display only a single content collection
     # singleCollection: 'main'
